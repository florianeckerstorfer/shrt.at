<!DOCTYPE HTML>
<html>
    <head>
        <title>Redirecting to {{ link.url }}</title>
        <meta http-equiv="Refresh" content="0; url={{ link.url }}" />
    </head>
    <body>
        <p>Redirecting to <a href="{{ link.url }}">{{ link.url }}</a>.</p>
    </body>
</html>